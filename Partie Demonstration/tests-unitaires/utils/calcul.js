const addition = (a, b) => a + b;

function soustraction(a, b) {
    return a - b;
}

function multiplication(a, b) {
    return a * b;
}

module.exports = {
    addition,
    soustraction,
    multiplication
};